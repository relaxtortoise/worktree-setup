version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell

run:
  tests: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck

formatters:
  enable:
    - goimports
    - gofmt
